# MatrixLab Environment Configuration (Client-side GitHub Pages deployment)
# VITE_GEMINI_API_KEY: Optional Gemini API key used directly by the client browser for the Matrix Scanner feature.
# Note: For static GitHub Pages deployments, client-side keys are bundled in the frontend.
VITE_GEMINI_API_KEY=
